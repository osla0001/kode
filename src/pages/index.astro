---
import "../styles/global.css";
import TextArea from "../components/TextArea.astro";
import Header from "../components/Header.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <Header />
  <TextArea />
</Layout>

<script>
  const burger = document.querySelector(".burger");
  const nav = document.querySelector("nav");
  const menu = document.querySelector(".menu");
  const header = document.querySelector("header");

  if (burger && nav && header) {
    burger.addEventListener("click", () => {
      burger.classList.toggle("active");
      nav.classList.toggle("active");
      header.classList.toggle("active"); // Tilføj eller fjern "active" på header

      // tilføjer eller fjerner "invert" på body baseret på nav's tilstand
      if (nav.classList.contains("active")) {
        document.body.classList.remove("invert"); // fjerner "invert", når menuen er åben
      } else {
        document.body.classList.add("invert"); // tilføjer "invert", når menuen lukkes
      }
    });
  }

  if (menu && burger && nav) {
    menu.addEventListener("click", () => {
      burger.classList.remove("active");
      nav.classList.remove("active");
      document.body.classList.add("invert"); // tilføjer "invert", når menuen lukkes
    });
  }
</script>

<style>
  body {
    height: 100vh;
  }
</style>
